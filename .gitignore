__pycache__/
*.pyc
.env
.venv/
venv/
.mypy_cache/
.pytest_cache/
.DS_Store
.idea/
.vscode/
dist/
build/
*.db
data/*.db
data/*.db-shm
data/*.db-wal

# Backup files created by ingestion pipelines
*.bak
data/entities/*.bak

# Raw external datasets (downloaded at setup time, too large to commit)
# - aourednik/historical-basemaps (~140MB, CC BY 4.0)
# - Natural Earth 10m shapefiles (~14MB, CC0)
# Re-fetch with: bash scripts/fetch_raw_data.sh
data/raw/aourednik-historical-basemaps/
data/raw/natural_earth/
data/raw/historical-basemaps/
data/processed/natural_earth_boundaries.json

# Runtime
*.log
server.log

# Claude Code local
.claude/

# Scratch dirs from one-off expansion / generation work
tmp_expansion/
scratch/
tmp_backport/

# v6.28: auto-generated briefings for accepted suggestions
# These are operational artefacts, not dataset — regenerated on each run.
data/briefings/
sdk-python/src/atlaspi_client.egg-info/

# HF dataset files — regenerable via prepare_export.py, not committed to keep repo light
hf-dataset/*.jsonl
hf-dataset/*.jsonl.gz

# SDK build artifacts
sdk-python/dist/
sdk-python/build/
sdk-js/dist/
sdk-js/node_modules/
sdk-js/package-lock.json

# Agent research staging (consumed by ingest scripts, not committed)
research_output/
static/app.js.bak

# v6.69 audit v4 — Wikidata HTTP cache (regeneratable via wikidata_bootstrap.py)
scripts/wikidata_cache/

# v6.99.29 autonomous loop — ChatGPT review logs (local auditing, sensitive)
data/chatgpt_review/
# LOOP_STATE.md is committed for transparency, but the loop session screenshots are not
data/enrichment/loop_screenshots/

# Password handoff files — must NEVER be committed
psw.txt
password.txt
*.password

# ETHICS-028 (2026-07-23): documenti di lavoro interni — mai più nel repo
# pubblico. Spostati in ../AtlasPI-internal/ (CLAUDE.md resta in locale:
# Claude Code lo legge dal filesystem, non da GitHub).
NEXT_SESSION_PROMPT.md
CLAUDE.md
docs/DISCOVERY.md
docs/academic/
docs/auto-iter*
docs/reddit-drafts.md
docs/outreach-draft.md
docs/structural-tracks-plan.md
docs/audit/FASE_A_B_HANDOFF.md
docs/audit/FASE_C_HANDOFF.md

# Prompt operativi per sessioni di agenti: istruzioni interne, non
# documentazione. RESUME_PROMPT conteneva anche IP del VPS, chiave ssh e
# path della key OpenAI. La trasparenza sul processo AI-assistito sta in
# METHODOLOGY §2.4 / ETHICS-028 / LOOP_STATE.md, non qui.
data/enrichment/RESUME_PROMPT.md
handoff/README.md
handoff/02-claude-code-prompt.md
handoff/benchmark/prompt.md
